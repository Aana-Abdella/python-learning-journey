# the program below takes a string input from the user and converts it to uppercase

user_input = input ("please enter the string you want to convert to uppercase:")

uppercase_string = user_input.upper()

print("The string in uppercase is:", uppercase_string)


# Write the program that takes a string input from the user and converts it to lowercase.

user_text = input(" Please enter the sring you want to convert to lowercase:")

lowercase_string = user_text.lower();

print("The string in lowercase is:", lowercase_string)

# Write the program that takes a string input from the user and converts it to title case.

user_string = input ("please enter the string you want to covert to title case:")

convert_to_title_case = user_string.title()

print("The string in title case is:", convert_to_title_case)