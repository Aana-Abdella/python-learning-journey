# Build a small program that represents the score of a game.

x, y, z = 10, 20, 30

total_score = x + y + z

# the difference between the scores of the players

score_difference = max(x, y, z) - min(x, y, z)

# product of the scores of the players
score_product = x * y * z

# Division involving the scores of the players
score_division = x / y / z

# An exponentiation calculation involving the scores of the players
score_exponentiation = x ** y ** z